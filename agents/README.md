# agents/

Agentic AI work built on LangChain / LangGraph, served against the platform's local models (Ollama) and data (CloudNativePG).

- **`nlp-to-sql/`** — natural-language-to-SQL agent.
- **`shared/`** — shared prompts, tools, and utilities reused across agents.
